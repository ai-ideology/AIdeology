{
  "name": "ai-ideology-z",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AIdeology — AI 意识形态测试 / AI ideology test web app",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "bun test",
    "assets": "bun run scripts/assets.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "playwright": "^1.63.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  }
}
